"""Contracts shared by the ingestion job and the inference API.

Kept deliberately small: only the things that *must* agree between the two components live here.
Anything one side can decide alone stays on that side.
"""
